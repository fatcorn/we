<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.den.we.mapper.UserMapper">


    <select id="retrieveUser" resultType="com.den.we.Vo.UserInfoVo">
        select mobile_phone mobilePhone, user_name userName, picture headUrl,country country, city city
          from  user
        where (mobile_phone = #{retrieveInfo} or user_name=#{retrieveInfo});
    </select>
</mapper>
